<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Allow index.php to handle all routes
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
